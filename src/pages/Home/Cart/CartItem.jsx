
const CartItem = ({item}) => {
    const {id, title, text, img, color, shadow, price, quantity} = item;
    return (
        <div>
            <h2>Item</h2>
        </div>
    );
};

export default CartItem;